# First-party native API. Use the public HTTPS /v1 endpoint, not a private
# server IP. The native session stores only API access/refresh tokens in the
# iOS Keychain / Android Keystore; never put a password in this file.
EXPO_PUBLIC_ZHANGAK_API_URL=https://platform.zhangak.com/v1
