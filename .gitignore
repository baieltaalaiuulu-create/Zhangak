# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/backend/node_modules
/backend/dist
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/backups/
*.sql.gz
*.dump

# Local text extraction of owner-provided PDFs. The source files live outside
# the repository and must not be copied into Git without explicit approval.
/docs/sources/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Large raw dataset archive
/sorted_data/

# Local QA exports and temporary processing work. These are deliberately kept
# outside release commits; published documentation belongs under docs/ instead.
/artifacts/
/scratch/
/sorted_data_preview/
/docs/screenshots/
/backend/scripts/materials_processor/
/PROMPT_FOR_NEXT_AGENT.md
/VERIFICATION_REPORT.md
